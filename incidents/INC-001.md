# INC-001 — NavV2 desktop: flash / FOUC en páginas internas

**Fecha apertura:** 2026-06-14  
**Fecha cierre (Etapa 1):** 2026-06-15  
**Fecha cierre (Etapa 3):** 2026-06-15  
**Branch Etapa 1:** `inc-001/etapa-1-navv2`  
**Branch Etapa 3:** `inc-001/etapa-3-navlinks`  
**Estado:** ✅ CERRADO (Etapas 1 + 3)  
**Confidence verifier:** ALTA  

---

## Síntoma

Nav desktop invisible ~1.4s al cargar cualquier página interna (`/certificados`, `/validacion-videos`, etc.). El nav "aparece" súbitamente después de la ventana de invisibilidad, produciendo un flash / FOUC perceptible.

Evidencia nivel 2: timeline Playwright confirmó `opacity ≈ 0.000` de `t=111ms` a `t=1393ms` en cada FPL a una página interna.

---

## Causa raíz (Etapa 1)

`NavV2.tsx` usaba `gsap.fromTo(navRef, {opacity:0, top:'-34px'}, {opacity:1, top:'14px', duration:1.1, delay:0.3, ease:'expo.out'})` en **todas las rutas** sin excepción. La ventana de invisibilidad total es 0.3s (delay) + 1.1s (animación) = **1.4s**.

La animación está diseñada para la landing (`/`) como entrada editorial. En páginas internas, el usuario ya conoce el nav — la animación solo introduce un período de FOUC sin valor UX.

Causas secundarias también resueltas:
- **RC-2:** `display: 'flex'` inline conflictuaba con `hidden md:flex` (Tailwind v4 `!important` lo resolvía accidentalmente — fragilidad estructural)
- **RC-3:** Guard de viewport `window.innerWidth >= 768` evaluado solo al montar, no reactivo a resize
- **RC-4:** Tween GSAP sin cleanup en E3 — corría contra nodo desmontado en FPL rápidos

---

## Solución implementada

**Archivo:** `components/v2/NavV2.tsx`

1. **Animation skip vía `usePathname`:** E3 bifurcado por ruta.
   - `pathname === '/' || pathname === null` → `gsap.fromTo` completo (experiencia editorial landing)
   - `pathname !== '/'` → `gsap.set(navRef.current, { opacity: 1, top: '14px' })` inmediato

2. **RC-2:** Eliminado `display: 'flex'` del inline style. Control de display exclusivamente por `className="hidden md:flex"`.

3. **RC-3:** `useIsMobile()` como dependencia de E3. Efecto E-RC3 separado para reset a `opacity:0` en resize a móvil.

4. **RC-4:** `gsap.killTweensOf(navRef.current)` en el return de E3.

---

## Validación

### Gate crítico (RED→GREEN)

| Test | Antes | Después |
|---|---|---|
| `gp5 S-02: animación GSAP <500ms en /certificados` | ❌ RED (maxOpacity≈0 en 200ms) | ✅ GREEN (gap=0ms, 3/3 estable) |

### Regresiones verificadas (GREEN → GREEN)

23 tests pasando post-fix. Sin regresiones en mobile nav, bleed, estructura, ni animación de landing.

### Evidencia visual nivel 2

| Escenario | Medición | Resultado |
|---|---|---|
| `/certificados` nav timing | opacity=1 en t≈200ms desde page start; gap GSAP = 0ms | Sin FOUC |
| `/` animación | Timeline: invisible t=0–962ms, expo.out t=1019–1611ms | Animación correcta |
| 390px | `display: none` en nav desktop | Sin bleed |

---

## Tests RED permanentes (no son regresiones de este ciclo)

| Test | Causa | Gate de |
|---|---|---|
| `gp1 LenisProvider restaura scroll` | FPL exclusivo, sin `<Link>`, BFCache desactivado | Deuda técnica (requiere soft nav) |
| `gp2 RC-1: nav PIERDE data-*` | NavV2 per-page — route group sin efecto en FPL | INC-002 cuando se introduzca `<Link>` |

---

## Deuda técnica generada

- **INC-002:** RC-1 (route group + `<Link>`) — diferido. Activar cuando se introduzca `<Link>` en links de ruta.
- **INC-002-navlinks-activeId:** RC-7b (conectar `activeId` al render visual de links) — prerequisito RC-7a cumplido en Etapa 3. Activar cuando se defina el diseño del estado activo.
- **INC-003-blockchain-flash-hash-scroll:** RC-9 (flash transitorio de Blockchain durante corrección de hash-scroll) — consecuencia estructural de RC-8, no regresión. Activar si el flash es reportado como perceptible en producción.

---

## Archivos modificados

| Archivo | Commit | Cambio |
|---|---|---|
| `components/v2/NavV2.tsx` | `261371e` | RC-2, RC-3, RC-4, animation skip (Etapa 1) |
| `tests/golden-paths/gp5-no-flash.spec.ts` | `0546574` | S-02 redesignado: MutationObserver + gap GSAP (Etapa 1) |
| `components/v2/LenisProvider.tsx` | `9751fc0` | RC-8: `return` incondicional → `if (!window.location.hash) return` (Etapa 3) |
| `components/v2/NavV2.tsx` | `9751fc0` | RC-7a: eliminados `acerca` y `roadmap` del mapa del IntersectionObserver (Etapa 3) |

---

## Etapa 3

### Síntoma (Etapa 3)

FPL a `/#proyectos` desde página interna aterriza en sección Blockchain en lugar de Proyectos. Cadena causal de 3 estados: (1) browser auto-scroll a `#proyectos` antes de GSAP (~50–200ms correcto) → (2) GSAP instala pin-spacer y desplaza el DOM; viewport queda apuntando a Blockchain (~400ms sin corrección) → (3) nunca llega a Proyectos sin intervención manual. Síntoma secundario: IntersectionObserver del nav incluía IDs `acerca` y `roadmap` sin correspondencia en el DOM (dead code silencioso desde rediseño v2).

### Causa raíz (Etapa 3)

- **RC-8:** `LenisProvider.tsx` línea 76 contenía `return` incondicional dentro del bloque `useEffect([pathname])`. Para cualquier FPL, `isFirstPath` es `true` y el return se ejecutaba antes de evaluar el hash — impidiendo que el mecanismo de corrección (`setTimeout(400ms) → ScrollTrigger.refresh() → lenis.scrollTo(hash)`) corriera en FPL con hash.
- **RC-7a:** `NavV2.tsx` mapa del IntersectionObserver incluía las keys `acerca` y `roadmap`. Ningún elemento con esos IDs existe en el DOM en ninguna ruta del sitio. Dead code inerte que oscurecía el rango real de `activeId`.

### Solución implementada (Etapa 3)

- **RC-8 (`LenisProvider.tsx:76`):** `return` → `if (!window.location.hash) return`. Una línea. En FPL sin hash el comportamiento es idéntico; en FPL con hash el bloque de corrección corre.
- **RC-7a (`NavV2.tsx:67–68`):** Eliminadas las entradas `acerca` y `roadmap` del mapa del observer. Dos líneas eliminadas. Cambio de comportamiento observable: cero.

### Validación (Etapa 3)

**Gate crítico (RED→GREEN):**

| GP | Test | Antes | Después |
|---|---|---|---|
| **GP-10** | FPL a `/#proyectos` aterriza en sección correcta | ❌ RED | ✅ GREEN |

**Suite completa post-fix:**

| GP | Test | Estado |
|---|---|---|
| GP-2 guards | Nav adjunto, count=1, Back/Forward | ✅ GREEN |
| GP-3 | Mobile nav (5 tests) | ✅ GREEN |
| GP-4 | Viewport bleed (4 tests) | ✅ GREEN |
| GP-5 | Nav structure + animation (5 tests) | ✅ GREEN |
| GP-1 Test 1 | LenisProvider restaura scroll | ❌ RED permanente (pre-existente) |
| GP-2 RC-1 | nav PIERDE data-* | ❌ RED permanente (pre-existente) |

Suite: **22/26 PASS** · 2 RED pre-existentes (deuda técnica) · 2 flaky de contención de Turbopack (pasan en re-run con workers reducidos — no son regresiones de este ciclo).

**Confidence verifier (Etapa 3):** ALTA. Verificación independiente con contexto fresco: GP-10 RED→GREEN pixel-perfect. Sin regresiones detectadas.

**Regresiones detectadas:** ninguna.

**Estado Etapa 3:** ✅ CERRADO.

**RC-9 documentado como candidato INC-003:** flash transitorio de Blockchain durante corrección de hash-scroll — 3 estados visuales, duración ~1.6–2s, reportado y confirmado por el usuario. No es regresión de Etapa 3 — consecuencia estructural del mecanismo de RC-8. Condición de activación cumplida; apertura a discreción del usuario. Ver `reports/FINAL_AUDIT_etapa3.md` § RC-9.

---

## Notas de workflow

- **Verifier:** RECHAZADO en primera corrida (S-02 flaky con `waitUntil:'commit'` + 200ms polling en server frío). Fix: S-02 redesignado a discriminador por duración GSAP (MutationObserver, threshold 500ms, inmune a compile time). Segunda corrida: APROBADO.
- **RC-1 (route group):** investigado y excluido del scope — solo beneficiaría soft navigation, inexistente en el sitio actual. Documentado para INC-002.
- **RC-6 descartado en Etapa 3:** premisa incorrecta — FooterV2 siempre incluye `id="contacto"`. `querySelector("#contacto")` siempre resuelve en todas las páginas. Lección documentada en `lore/testing.md`.
- **RC-7b diferido:** prerequisito RC-7a cumplido. Activar como INC-002 cuando se defina diseño del estado activo de links.
